from setuptools import setup, find_packages

setup(
    name="PPO_Library",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # list any dependencies here, e.g., "pandas", "numpy"
    ],
    python_requires=">=3.8",
)